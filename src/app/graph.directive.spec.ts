import { GraphDirective } from './graph.directive';

describe('GraphDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphDirective();
    expect(directive).toBeTruthy();
  });
});
